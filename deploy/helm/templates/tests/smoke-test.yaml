apiVersion: v1
kind: Pod
metadata:
  name: {{ include "cartlabs.fullname" . }}-smoke
  labels:
    {{- include "cartlabs.labels" . | nindent 4 }}
    app.kubernetes.io/component: smoke-test
  annotations:
    helm.sh/hook: test
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  automountServiceAccountToken: false
  securityContext:
    runAsNonRoot: true
    runAsUser: 65534
    seccompProfile: { type: RuntimeDefault }
  containers:
    - name: smoke
      image: curlimages/curl@sha256:463eaf6072688fe96ac64fa623fe73e1dbe25d8ad6c34404a669ad3ce1f104b6
      command:
        - sh
        - -ec
        - |
          curl --fail --silent --show-error http://{{ include "cartlabs.fullname" . }}-api:8080/api/v1/health/ready | grep -q '"status":"ok"'
          {{- if .Values.web.enabled }}
          curl --fail --silent --show-error http://{{ include "cartlabs.fullname" . }}-web:3000/ | grep -q 'Cartlabs'
          {{- end }}
          {{- if .Values.observability.enabled }}
          curl --fail --silent --show-error http://{{ include "cartlabs.fullname" . }}-prometheus:9090/-/ready >/dev/null
          curl --fail --silent --show-error http://{{ include "cartlabs.fullname" . }}-tempo:3200/ready >/dev/null
          {{- if .Values.observability.grafana.enabled }}
          curl --fail --silent --show-error http://{{ include "cartlabs.fullname" . }}-grafana:3000/api/health | grep -q '"database"'
          {{- end }}
          {{- end }}
          {{- if .Values.syntheticTraffic.enabled }}
          curl --fail --silent --show-error http://{{ include "cartlabs.fullname" . }}-synthetic-traffic:9094/health/ready >/dev/null
          {{- end }}
      securityContext:
        allowPrivilegeEscalation: false
        capabilities: { drop: ["ALL"] }
        readOnlyRootFilesystem: true
