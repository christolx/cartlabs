{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cartlabs.serviceAccountName" . }}
  labels:
    {{- include "cartlabs.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
