---
- name: Restart k3s
  ansible.builtin.systemd_service:
    name: k3s
    state: restarted
    daemon_reload: true

- name: Apply k3s sysctl
  ansible.builtin.command: sysctl --system
  changed_when: true
